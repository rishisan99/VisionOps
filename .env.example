# Use unique values when running multiple projects at once.
# Copy this file to `.env` and adjust as needed.

# Host ports (left side of Docker port mapping)
BACKEND_PORT=8002
FRONTEND_PORT=8502

# Frontend -> backend URL inside Docker network.
# Keep this as-is unless you are running frontend outside Docker.
API_URL=http://backend:7000
